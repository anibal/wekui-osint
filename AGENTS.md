<!--
Curation policy — from our 234-run behavioral A/B on the stock Phoenix AGENTS.md
(~/Sandboxes/Personal/phoenix-usage-rules): always-on lines are reserved for
decisions an agent cannot derive — arbitrary defaults, project conventions,
contradiction fixes. Rules that models already follow without being told were
deleted or delegated: framework reference lives in on-demand skills
(.claude/skills/, synced from the installed deps by `mix usage_rules.sync`),
web-layer rules load only when web files are touched (.claude/rules/web.md).
An emphasis file works on what it names; keep this one short and name only
what moves behavior.
-->

## Project outline (docs/)

The project's knowledge is a linked outline under `docs/pages/`, mapped from
[`docs/pages/index.md`](docs/pages/index.md): first principles, the ubiquitous
language (concepts + policies), search strategy, architecture stances, a dated
append-only decision log, open questions, and research. **Always** read the index
plus the pages relevant to your task. For domain work the vocabulary is the
contract: use those exact terms — with the same meanings — when discussing,
planning, implementing, and reporting back, and update the pages in the same
change that changes the code.

`docs/` is an outl workspace (outl.app: an outliner whose op-log treats the markdown
as a projection — it can rewrite these files). Non-negotiable rules: every bullet in
`docs/pages/*.md` is ONE line — never hard-wrap inside a bullet; prose documents live
at `docs/` root and are never moved into `docs/pages/` (outl would bulletize them);
`docs/ops/` and `docs/.outl/` are gitignored machinery, everything else is committed.

## What this is

A Phoenix v1.8 web application whose domain layer is Ash Framework on SQLite
(`ash_sqlite`). Domain logic lives in Ash resources under `lib/wekui/`
(domains: `Wekui.Core`, `Wekui.Acquisition`, `Wekui.Capture`); the web layer lives under
`lib/wekui_web/`.

## Standing decisions

These are project decisions, not general knowledge — you cannot derive them
from priors or from the code:

- When you finish a set of changes, run `mix precommit` and fix everything it
  reports.
- HTTP client: the already-included `Req`. **Never** add or use `:httpoison`,
  `:tesla`, or `:httpc`.
- **Never** add dependencies without asking first. One pre-approved exception:
  `date_time_parser` for parsing dates/times.
- `Task.async_stream/3` calls **always** pass `timeout: :infinity`.
- The domain is Ash-first: model behavior as Ash resources and actions. Do not
  write Ecto schemas, changesets, or hand-rolled `Repo` calls for domain data.
  - Scaffold with the `mix ash.gen.*` generators first, then hand-edit.
  - Database changes go through `mix ash.codegen <change_name>` — **never**
    hand-write migrations.
- The database is SQLite. Do not use Postgres-only SQL or Ecto features.

## Where the rest of the knowledge lives

Do not inline framework documentation into this file — load it on demand:

- `.claude/rules/web.md` — web-layer and design conventions; loads
  automatically when you work with `lib/wekui_web/**` or `assets/**`.
- `.claude/skills/` — framework rules synced from the installed dep versions:
  `ash-framework`, `phoenix-liveview`, `phoenix-heex`, `phoenix-core`.
  Skills marked `managed-by: usage-rules` are regenerated by
  `mix usage_rules.sync` — **never** hand-edit them; after a dep upgrade,
  re-run the sync so the rules track the code.
- Package docs search: `mix usage_rules.search_docs "term" -p ash` (any dep).

## Growing this system

When we settle a new durable convention, put it in exactly one place:

- Cross-cutting and non-derivable → one bullet in this file.
- Web-layer only → `.claude/rules/web.md`.
- A workflow, checklist, or reference topic → a new hand-written skill in
  `.claude/skills/<name>/SKILL.md` (safe alongside the managed ones).
- A domain concept → its page in `docs/pages/` plus its hub's index line, in the
  same change. A decision settled by evidence → a dated `decision-YYYY-MM-DD-*`
  page; a question we cannot answer yet → an `open-*` page (with `opened::`);
  measurements → a dated `research-YYYY-MM-DD-*` page; a value the system obeys →
  a `principle-*` page; collection doctrine → `search-strategy`; system-shape
  stances → `architecture`. Slug prefix, `type::`, and hub membership must agree.
